echo "Striping $HOME/$1"
find $HOME/$1/lib -type f \( ! -iname "*.h" ! -iname "*.hpp" ! -iname "*.ttl" ! -iname "*.la" ! -iname "*.pyc" ! -iname "*.pc" ! -iname "*.py" \) |while read file; do strip "$file"; done
strip $HOME/$1/bin/*
echo done
